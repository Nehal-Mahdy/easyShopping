import { CurrencyEgPipe } from './currency-eg.pipe';

describe('CurrencyEgPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyEgPipe();
    expect(pipe).toBeTruthy();
  });
});
